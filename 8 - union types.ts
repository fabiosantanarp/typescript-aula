function somar(num1:number|string, num2:number) {
    return num1 + num2;
}
let numero = somar(1, 2);

// if (typeof num1 === 'number')
//     return num1 + num2;
// else
//     return `${num1}${num2}`